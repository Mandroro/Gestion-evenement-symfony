{% extends 'base.html.twig' %}

{% block title %}Statistique{% endblock %}

{% block body %}
    <h1>Statistique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statistique.id }}</td>
            </tr>
            <tr>
                <th>Total_participants</th>
                <td>{{ statistique.totalParticipants }}</td>
            </tr>
            <tr>
                <th>Total_revenue</th>
                <td>{{ statistique.totalRevenue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statistique_index') }}">back to list</a>

    <a href="{{ path('app_statistique_edit', {'id': statistique.id}) }}">edit</a>

    {{ include('statistique/_delete_form.html.twig') }}
{% endblock %}
