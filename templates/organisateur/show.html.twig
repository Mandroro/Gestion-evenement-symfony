{% extends 'base.html.twig' %}

{% block title %}Organisateur{% endblock %}

{% block body %}
    <h1>Organisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ organisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ organisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ organisateur.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ organisateur.adresse }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ organisateur.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organisateur_index') }}">back to list</a>

    <a href="{{ path('app_organisateur_edit', {'id': organisateur.id}) }}">edit</a>

    {{ include('organisateur/_delete_form.html.twig') }}
{% endblock %}
